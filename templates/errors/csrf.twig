{% extends 'layout/base.twig' %}
{% block content %}
<div class="text-center py-24">
  <div class="text-7xl mb-4">⏰</div>
  <h1 class="font-display text-4xl font-extrabold text-slate-900">Session expired</h1>
  <p class="text-slate-600 mt-3">Please try again — your session or CSRF token has expired.</p>
  <a href="{{ url('/') }}" class="btn btn-primary mt-8">← Back to home</a>
</div>
{% endblock %}
